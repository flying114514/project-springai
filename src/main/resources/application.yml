spring:
  application:
    name: project-springai
app:
  storage:
    endpoint: http://192.168.88.140:9000
    access-key: ${RUSTFS_ACCESS_KEY}
    secret-key: ${RUSTFS_SECRET_KEY}
    bucket: interview-guide
    region: us-east-1
